import { createSignal } from "solid-js";

// interface Button {
//   title: string;
//   component: OverridableComponent<SvgIconTypeMap<{}, "svg">>;
// }

// const buttons: Button[] = [
//   { title: "Tables", component: Folder },
//   { title: "Fields", component: Label },
//   { title: "Views", component: Web },
//   { title: "Scripts", component: Code },
// ];

// const buttons2: Button[] = [{ title: "Settings", component: Settings }];

function SideBar() {
  // const [selectedListItems, setSelectedListItems] = createSignal<boolean[]>(
  //   buttons.map((_, ind) => ind == 0)
  // );
  // const [selectedListItems2, setSelectedListItems2] = createSignal<boolean[]>(buttons2.map(() => false));

  return <></>;
}

export default SideBar;
